{
  "name": "a11y-guidelines",
  "version": "2.0.0",
  "description": "Recommandations accessibilité numérique Orange",
  "repository": "https://github.com/Orange-OpenSource/a11y-guidelines",
  "main": "index.js",
  "scripts": {
    "build:dev": "npm run clean && ELEVENTY_ENV=development eleventy",
    "serve:dev": "npm run clean && ELEVENTY_ENV=development eleventy --serve --quiet",
    "build:prod": "npm run clean && ELEVENTY_ENV=production eleventy",
    "serve:prod": "npm run clean && ELEVENTY_ENV=production eleventy --serve --quiet",
    "clean": "del-cli _site",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Orange SA",
  "license": "CC-BY-SA-3.0",
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "cheerio": "^1.0.0-rc.3",
    "del-cli": "^3.0.0",
    "markdown-it": "^11.0.0",
    "markdown-it-anchor": "^5.3.0",
    "string": "^3.3.3"
  },
  "dependencies": {
    "tarteaucitronjs": "^1.4.0"
  }
}
