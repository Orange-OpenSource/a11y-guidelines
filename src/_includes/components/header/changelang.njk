<ul class="navbar-nav">
  {% for lang in site.locales.available %}
    {% set active = locale == lang %}
    <li class="nav-item">
      <a href="/{{ lang }}/" class="nav-link{% if active %} active{% endif %}"{% if active %} aria-current="true"{% endif %}>
        {{ lang | upper }}
      </a>
    </li>
  {% endfor %}
</ul>
