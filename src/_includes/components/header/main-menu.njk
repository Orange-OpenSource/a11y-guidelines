{% import "../ui.njk" as ui with context %}

<ul id="main-menu" class="navbar-nav">
  {% for link in header.mainNavigation[locale] %}
    <li class="nav-item">
      {{ ui.navLink(link) }}
    </li>
  {% endfor %}
</ul>
